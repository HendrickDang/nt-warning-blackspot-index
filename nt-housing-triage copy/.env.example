# Local parser model (optional). The app works without these — it falls back
# to the deterministic parser in lib/parser/fallback.ts.
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=nt-housing-triage
OLLAMA_TIMEOUT_MS=20000
